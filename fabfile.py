

def create_project(project_name, package_name='', project='java'):
    commands = []
    commands.append('mkdir -p %s/src/main/java' % project_name)
    commands.append('mkdir -p %s/src/test/java' % project_name)
    commands.append('touch -p %s/pom.xml' % name)
    
    # replace . with /

    # create pom.xml as the template

    # create $NameApp.java as the template

    # create $NameAppTest.java as the template

    